-- =====================================================================
-- 0058 : เพิ่มชนิดเอกสาร "ใบส่งของ"
--
--  ไฟล์นี้มีคำสั่งเดียวโดยตั้งใจ
--  PostgreSQL ใช้ค่า enum ที่เพิ่งเพิ่มภายในทรานแซกชันเดียวกันไม่ได้
--  ถ้ารวมกับ 0059 จะได้ error "unsafe use of new value"
--  ต้องรันไฟล์นี้ให้จบก่อน แล้วค่อยรัน 0059
-- =====================================================================
alter type doc_kind add value if not exists 'delivery_order';
